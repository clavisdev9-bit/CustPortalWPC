# Relative by default -- proxied to the backend by Vite in dev (see vite.config.js) and by the
# reverse proxy in production. Only set an absolute URL if the API truly lives on another origin.
VITE_API_BASE_URL=/api/v1
